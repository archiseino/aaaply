{
  "name": "sobatapply-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../dist && mv dist ../dist",
    "install": "cd frontend && npm install"
  }
}
